<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.vic19.dao.CityDao">
    <select id="getCityId" parameterType="String" resultType="Integer">
        SELECT
            c.id_city
        FROM city c
        where c.status=1
        and city =#{cityName}
    </select>

    <resultMap type="bo.ucb.edu.vic19.dto.LocationRequest" id="citiesMap">
        <result column="idLocation" property="idLocation"/>
        <result column="locationName" property="locationName"/>
        <result column="longitude" property="latitude"/>
        <result column="latitude" property="latitude"/>
    </resultMap>
    <select  id="cities" resultMap="citiesMap">
        SELECT id_city idLocation, city locationName, lon longitude, lat latitude
        FROM city
        WHERE city.status=1
    </select>
</mapper>