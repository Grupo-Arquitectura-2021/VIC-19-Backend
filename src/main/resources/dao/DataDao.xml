<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.DataDao">
    <insert id="createNewData" parameterType="java.util.List">
        INSERT INTO covid_data
        VALUES
        <foreach collection="list" item="element" index="index" open="(" separator="),("  close=")">
            null,
            #{element.idPageUrl},
            #{element.idCountry},
            #{element.deathCases},
            #{element.confirmedCases},
            #{element.vaccinated},
            #{element.cumulativeCases},
            #{element.recuperated},
            #{element.date},
            #{element.status},
            #{element.transaction.txDate},
            #{element.transaction.txIdUser},
            #{element.transaction.txHost},
            #{element.transaction.txUpdate}
        </foreach>
    </insert>
</mapper>